# Development-only quality gates. Generate requirements-dev.lock with:
#
#   pip-compile --allow-unsafe --generate-hashes \
#       --output-file=requirements-dev.lock --strip-extras requirements-dev.txt
#
# Runtime/test compatibility is constrained by the existing exact lock.
-c requirements.lock

# Full repository tests exercise optional integrations with fake/offline
# clients. Production installs select these explicitly with the wheel extras.
anthropic>=0.34,<1.0
build>=1.3,<2.0
httpx2>=2.9,<3.0
mcp>=1.28,<2.0
mypy>=1.17,<2.0
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=6.2,<8.0
pytest-xdist>=3.6,<4.0
ruff>=0.12,<1.0
setuptools>=69,<82
wheel>=0.45,<1.0
