# Capability Ledger

> Updated: 2026-08-30
> Authority: this is the single current capability-status ledger. Its
> authority is `docs/PLATFORM_CHARTER.md` (§6 invariants) and named decisions
> in `docs/DECISIONS.md`; this ledger cannot authorize a feature or change an
> invariant.
>
> Historical roadmap/status documents are evidence, not current capability
> claims. When they disagree with this table, use the authority order:
> charter invariants + `docs/DECISIONS.md` → code/tests → this ledger →
> `docs/product/ROADMAP.md` → `docs/archive/` historical roadmap/status text.

## Field meanings

- `implemented`: corresponding runtime code or operator artifact exists.
- `test-only`: the capability is deliberately limited to fake/local test
  evidence and is not an operable end-to-end surface.
- `default-enabled`: a clean configuration activates it without an opt-in flag
  or operator action.
- `deployed`: this repository has direct evidence that the capability is
  currently installed in a runtime environment. `unknown` is not `yes`.
  Personal pilot evidence may establish `deployed=yes` when the repository
  contains direct evidence that the capability is installed and enabled in
  the pilot runtime; such evidence must be explicitly scoped as
  `personal-pilot deployment only` and must not establish `canary-proven`
  or `production-ready` (DG-PERSONAL-PILOT-v1 D1 clarification, 2026-08-25).
- `canary-proven`: the plan's real-environment/time-window gate has passed.
  Local tests are never substituted for canary evidence.
- `production-ready`: all required implementation, deployment, canary,
  recovery and safety gates for that capability are satisfied.

Allowed values are `yes`, `no`, `unknown`, and `n/a`. A code-complete feature
may still correctly have `production-ready=no`.

## Current ledger

| Capability ID | implemented | test-only | default-enabled | deployed | canary-proven | production-ready | Evidence / blocker |
|---|---|---|---|---|---|---|---|
| `phase0_release_gate` | yes | no | yes | unknown | n/a | no | Local Python 3.10 exact-lock check, compile, static invariant checks, diff check and the current 3,851-test offline suite pass; the CI workflow has not yet produced remote run evidence for this worktree. |
| `core_control_plane` | yes | no | yes | unknown | unknown | no | FastAPI/SQLite monitor, scheduler, approvals and APIs exist. WP-1C removed the false-terminal stop defect; WP-2A adds default-off single-leader ownership. WP-6A adds explicit `all/api/scheduler/worker` roles, role-based readiness, full scheduler refusal for a losing durable-lease contender, supervised readiness and evidence-backed JSON operational metrics. Ambiguous launch, default-off rollout and missing real drills still prevent a production-ready claim. |
| `approval_and_audit` | yes | no | yes | unknown | unknown | no | Approval/audit boundaries remain tested; legacy `append_audit()` is still best-effort/append-only, while migrations 2–3 add a versioned hash-chained durable `audit_events` ledger plus atomic retry/dead-letter JSONL export outbox. Ordinary, prewarm, standalone enqueue, validation and native/unbound Job materialization now write bounded durable events in their UoWs; v1 and v2 ExecutionPlan request, stale rejection, and one-Job materialization events are explicitly catalogued and commit with their owning transaction without copying command text. Service-account/token approval decisions, dataset cache reconciliation, project-instance reconcile, project candidate/instance lifecycle, canonical ProjectVersion creation, inventory-scan and ignore-nested batch decisions, import/ignore candidate decisions, identity membership grant/update/remove decisions, Project bootstrap, standalone Environment, Run Template, Project Defaults, Dataset asset adoption, Dataset alias revision, Dataset sharing offer/accept, and grant withdrawal materialization, and approval-gated `git_init`/`project_deploy`/`apply_patch` plus direct `hub_sync`, approval-gated `server_bootstrap`, revision-scoped attempt filesystem preflight, and coding-run cleanup now commit bounded mutation/observation events atomically as well. Approval-gated Run Profile and Dispatch Policy revision inserts, project experiment-record create/update/delete, queued-Job cancellation, and generic reconcile terminal/requeue/blocked transitions remain covered. `/events`/`/audit` mark durable versus legacy evidence and expose partial adoption. The typed adoption catalog now has 79 entries, including 16 explicit compatibility/inventory entries for every remaining literal legacy action; the CI gate rejects newly unclassified literal writers and durable actions routed to JSONL. `LEGACY_AUDIT_JSONL_ENABLED` is default-on and can suppress only the already-durable standalone-enqueue, unbound-coding, unpinned-server, and dataset-cache summaries; durable events and reject/error evidence remain on. The read-only `scripts/audit_export_status.py --require-clear` command and `/operations/metrics` share a deterministic projection: backlog is `attention`, and exhausted dead-letter rows activate a critical local alert signal. External notifier/retention/anchor evidence remains open, and no deployment/canary evidence exists. |
| `ssh_execution_v1` | yes | no | yes | unknown | unknown | no | Existing SSH/SFTP/tmux/sentinel backend remains the supported default. Legacy stop preserves its immutable intent/terminal-evidence semantics; the rollout-gated generic attempt path now adds durable attempt identity, prepare/launch/stop/collect operations, and evidence-driven ambiguity handling. |
| `codex_exec_runner_v1` | yes | no | no | unknown | unknown | no | Approval-gated Codex exec path exists but requires configured Runner infrastructure and has no current deployment/canary evidence. |
| `oidc_identity` | yes | no | no | no | no | no | Dependency-complete local implementation; default off and explicitly recorded as not deployed. |
| `authorization_shadow` | yes | no | no | no | no | no | The observational `shadow` adapter and evidence path are implemented locally; the clean default remains `off`, and no deployment or Canary evidence is claimed. |
| `authorization_enforcement` | yes | no | no | no | no | no | Fail-closed HTTP/local-tool enforcement, project collection filtering, service scopes, high-risk self-decision denial, and `off`, `shadow`, and `enforce` settings validation are implemented and locally tested. The default remains `off`; `DG-PRODUCT-V2-BASELINE-v1` approves a future 117 non-production Pilot rollout but is not deployment or Canary evidence. |
| `api_v2_foundation` | yes | no | no | no | no | no | `DG-API-V2-FOUNDATION-v1` adds an empty root `/api/v2` router boundary, default-off runtime gate, stable APIError/request-ID primitives, bounded versioned cursor codec, and migration v5 completed-result idempotency UoW. Later Product endpoints are independently gated and tracked by their own capability rows; local tests are not deployment or Canary evidence. |
| `product_rbac_v2` | yes | no | no | no | no | no | `DG-PRODUCT-RBAC-V2-v1` adds migration v6 independent multi-role bindings, dynamic readiness, explicit no-hierarchy action sets, actor-safe legacy mapping, high-risk two-person role-change approvals, opaque cross-Project decision lookup, and atomic audit/idempotency evidence. `PRODUCT_RBAC_V2_ENABLED` and its `API_V2_ENABLED` dependency remain default off; no deployment or Canary evidence exists. |
| `project_bootstrap_v2` | yes | no | no | no | no | no | `DG-PROJECT-BOOTSTRAP-V2-v1` adds Migration v7 with same-Project exact-reference foreign keys, canonical typed bootstrap payloads, pure previews, high-risk two-admin approvals, verified v2 approval review detail, atomic Project/roles/Environment/Run Template/Defaults materialization, a privacy-bounded Project Workspace, and the Project Wizard. Typed lineages reject legacy raw successors. `PROJECT_BOOTSTRAP_V2_ENABLED` and both Product v2 dependencies remain default off. Dataset grant/alias elements remain opaque and unavailable pending PR-07/08; no Git/deploy/SSH side effect, deployment, or Canary evidence exists. |
| `project_environments_v1` | yes | no | no | no | no | no | `DG-PROJECT-ENVIRONMENTS-V1` adds strict immutable `host-environment-v1` revisions and canonical `environment-change-v2` create/update/archive approvals over Migration v7. Request and approve boundaries revalidate setup safety, actor/RBAC state, exact head and immutable bytes; archive remains available after policy drift. Readiness is a bounded, read-only projection of verified active server-publication evidence and fresh observations, with missing/stale/runtime-only evidence reported as `unknown`. Secret values are rejected through a route-scoped non-reflecting validation envelope and never persisted or logged. `PROJECT_ENVIRONMENTS_V1_ENABLED` depends on API v2 and Product RBAC and remains default off; no deployment or Canary evidence exists. |
| `run_template_v2` | yes | no | no | no | no | no | `DG-RUN-TEMPLATE-V2` adds strict `run-template-spec-v2`, deterministic shell-free argv compilation, explicit legacy adoption, immutable typed successors and exact-reference `project-defaults-v1` history over unchanged Migration v7 tables. Canonical fractional numbers are exponent-free decimal strings; JSON floats and coercion fail closed. Both standalone mutation kinds are high-risk, digest-bound, idempotent, two-person transactions with bounded durable audit. Legacy raw heads expose metadata only and Product execution consumes typed heads only through ExecutionPlan v2. `RUN_TEMPLATE_V2_ENABLED` depends on API v2, Product RBAC, and Host Environments and remains default off; no deployment or Canary evidence exists. |
| `dataset_assets_v2` | yes | no | no | no | no | no | `DG-DATASET-ASSETS-V2-v1` adds Migration v8's six-table governance schema, explicit digest-bound legacy snapshot adoption, Project-owned assets, immutable expected-head alias revisions, serialized cycle-safe lineage insertion, and bounded detail/lineage/usage/storage read models. Legacy unscoped rows are never enumerated or backfilled; storage paths/source identity are redacted. Usage now projects only verified exact ExecutionPlan v2 bindings; `project-defaults-v1` correctly has an exhaustive empty Dataset-reference result. `DATASET_ASSETS_V2_ENABLED` depends on API v2 and Product RBAC and remains default off; no deployment or Canary evidence exists. |
| `dataset_sharing_v2` | yes | no | no | no | no | no | `DG-DATASET-SHARING-V2-v1` implements digest/expiry-bound source offers, independent target acceptance, atomic exact-snapshot grants, monotonic source revoke/target unlink, target-scoped aliases, and fail-closed cross-Project list/detail/lineage/usage/storage projections over unchanged Migration v8. All three mutation kinds are transaction-only high-risk approvals with two-person Project-local roles, bounded durable audit, and idempotent HTTP decisions. `DATASET_SHARING_V2_ENABLED` depends on API v2, Product RBAC, and Dataset Assets and remains default off; no deployment or Canary evidence exists. |
| `dataset_publish_v2` | yes | no | no | no | no | no | `DG-DATASET-PUBLISH-V2-v1` implements deterministic read-only local-path and exact Run-output previews, strict digest-bound `dataset_publish_v2` two-person approval, secure no-follow source scanning/building, content-addressed interrupted-build resume, and one-transaction snapshot/asset/initial-alias/lineage/audit finalization over unchanged Migration v8. Local identities and all API/audit errors redact absolute paths; Run publication requires exact typed completion and governed input-snapshot evidence. `DATASET_PUBLISH_V2_ENABLED` depends on API v2, Product RBAC, Dataset Assets, and both snapshot gates, remains default off, and has no deployment or Canary evidence. |
| `execution_plan_v2` | yes | no | no | no | no | no | `DG-EXECUTION-PLAN-V2-v1` adds Migration v9's immutable exact-spec companion, read-only preview, digest-bound idempotent submit, historical alias and exact grant pinning, deterministic resource/target selection, two-person approve-time revalidation, one-Job materialization, and closed approval-envelope to Job/Attempt semantic-version mapping. Dataset usage is bounded and Project/snapshot scoped; foreign Project Run routes are opaque. `RUN_EXPERIENCE_V2_ENABLED` depends on API v2, Product RBAC, Host Environments, Run Templates, and Dataset Assets, remains default off, and has no deployment or Canary evidence. Migration 17 (2026-08-29) widened the two BEFORE INSERT consistency triggers on `execution_plan_v2_specs`/`experiment_plan_specs` from `instance.state = 'available'` to `instance.state IN ('available', 'diverged')`, matching the resolver's and `finalize_project_instance_update_decision`'s already-accepted states -- a diverged-but-exact-commit instance no longer raises `sqlite3.IntegrityError` at request creation. |
| `product_run_experience_v2` | yes | no | no | no | no | no | `DG-PRODUCT-RUN-EXPERIENCE-V2-v1` keeps `execution_plans.id` as the only Product Run identity and projects a closed state from verified approval/Job/attempt/operation/collection evidence without persisting a second Run state. Five gated v2 routes provide safe detail/timeline, exact-reference Clone preview, bilateral read-only Compare, approval-backed Stop intent, and validated metadata-only Artifacts. Stop request/decision/idempotency/audit/outbox changes are atomic; semantic dedup serializes across connections and never directly terminalizes a Job. Workspace gate-on cards use the ExecutionPlan projection, preserve unknown/legacy/partial evidence, and render paths through text nodes. `RUN_EXPERIENCE_V2_ENABLED` remains default off; no Migration v10, deployment, Pilot, Canary, or production-readiness evidence exists. |
| `assistant_claude_turn` | yes | no | no | yes | no | no | DG-ASSISTANT-CLAUDE-TURN v1 (2026-08-26): runner-hosted zero-tool `claude -p` chat turns for the assistant with deterministic brain routing (runner-claude → vLLM → rule-based) and explicit Chinese degradation reasons; AI provider status panel (claude/codex runner probes, Anthropic API key UI management with 0600 atomic env writes, key never returned/audited). Activates only when the runner has an authenticated claude CLI; the exact CLI flag shape is not yet exercised against a real CLI (fails closed to rule-based). Deployed on the personal pilot; no canary/production claim. |
| `assistant_tools_v1` | yes | no | no | no | no | no | DG-ASSISTANT-TOOLS v1 (2026-08-30, T-1…T-7; packets P1a/P1b): per-turn `dat_` assistant tokens (migration 18 `assistant_turn_tokens`, digest-only, bound to the speaking human/legacy actor, TTL = turn timeout, revoked in `finally`, confined by a derived route allowlist to exactly the routes behind the 25 MCP bridge tools, never accepted by `/ws`); `app/mcp_bridge.py --stdio` runner mode with per-turn call cap and call log; runner-claude turns ship the bridge over SFTP and launch `claude -p --mcp-config … --strict-mcp-config --allowedTools mcp__dispatch__*`, degrading to the zero-tool turn with an explicit reason when the runner lacks python/mcp/httpx; tool calls render as `tool_note`/`approval_card` frames. `ASSISTANT_TOOLS_V1_ENABLED` default off and requires `ASSISTANT_TOOLS_DISPATCH_BASE_URL` (a URL the runner can reach Server A at) plus `mcp`+`httpx` on the runner's `ASSISTANT_TOOLS_RUNNER_PYTHON`. Suites `tests/test_assistant_tokens.py`, `tests/test_assistant_turn_token_auth.py`, `tests/test_mcp_bridge.py` (stdio), `tests/test_assistant_turns.py`, `tests/test_assistant_ws_routing.py` are green. Not yet enabled on the pilot: enabling needs the runner-side python packages and a reachable base URL (no real `claude -p --mcp-config` evidence recorded). |
| `agent_runtime_v3` | yes | no | no | no | no | no | DG-AGENT-RUNTIME-V3 v1 (2026-08-30, Phase 1a S1–S2c): runner-side `dispatch_agent/` package (own wheel `dispatch-agent`; config/credential/Claude token in 0600 files under `~/.config/dispatch-agent/`, `scrub_environment()` keeps them out of the SDK subprocess; outbound-only WebSocket with `dar_` credential; INV-AGENT-2 `decide_tool_use` matrix — workspace file tools confined by realpath, validation allowlist direct, every other Bash prompts; `SessionHost` around `ClaudeSDKClient` with `can_use_tool`, streaming, interrupt, resume; git mirror+worktree workspace; agent card probe; bundled `mcp_bridge.py` mirror). Server A: migration 19 (`agent_runners`, `agent_session_runtime`, `agent_session_events`, `agent_permission_requests`), `agent_runner_enroll`/`agent_runner_revoke` approval kinds, `AgentGateway` (`WEBSOCKET /agent-runner/ws`, own interface class, fail-closed auth, A2A task states, append-only event log with server sequence numbers, permission prompts persisted + audited, disconnect → `unknown`), Studio routes under `/api/v2/studio/*` (404 while `AGENT_RUNTIME_V3_ENABLED` is off), session-scoped `dat_` platform-tool token shipped in `session/open` and revoked on close. Suites `tests/test_dispatch_agent_*.py`, `tests/test_agent_gateway.py`, `tests/test_agent_runners.py`, `tests/test_mcp_bridge_mirror.py` green. Not deployed: pilot runner install and the end-to-end acceptance round in `docs/runbooks/RUNNER_AGENT_SETUP.md` are still pending, so no real-SDK evidence exists. |
| `studio_ui_v1` | yes | no | n/a | no | no | no | DG-STUDIO-UI v1 (2026-08-30, Phase 1a S3): `studio/` React 19 + TypeScript + Vite + Tailwind SPA built to `static/studio/` (gitignored; CI `npm ci && npm run build && npm test`, pilot deploy builds + rsyncs) and served under the public `/static/` prefix — no new auth exemption; login card until `/auth/me` resolves. Five-entry rail, project/session page with persisted-event stream (replay + live over `/api/v2/studio/sessions/{id}/stream`), inline workspace permission prompts, approval cards decided in place, runner enrollment. `scripts/frontend_smoke.py --require-studio` and `check_wheel_boundaries.py` pin the self-contained build and packaging; Vitest covers the transcript projection and login gate. Not yet deployed to the pilot; no browser end-to-end evidence recorded. |
| `metrics_v1` | yes | no | no | yes | no | no | DG-METRICS-CONTRACT v1 (2026-08-24, Option A): metrics-v1 file contract implemented — pure fail-closed parser (≤64 KiB, ≤256 keys, closed key charset, JSON floats rejected, canonical-decimal strings typed `decimal`), additive migration 13 (`run_metrics` + `run_metrics_collection` four-state collected/missing/invalid/oversize evidence, `jobs` untouched), flag-gated local parse in the job-finish hook after the existing rsync pull (zero new remote commands; failures never affect job status/reconciliation/scheduling), read-only `GET /jobs/{job_id}/metrics`, and Product Run `metrics_status` projection. `METRICS_V1_ENABLED` default off; enabled on the personal pilot 2026-08-25 — personal-pilot deployment only — with end-to-end evidence: pilot job 94 (server-a) wrote `metrics.json`, the job-finish hook parsed it, `run_metrics_collection` recorded `collected` with a matching sha256, all four value types (`decimal`/`int`/`bool`/`string`) landed in `run_metrics`, and the `metrics_collected` audit event was written; job status was unaffected. No canary/production-readiness claim. |
| `claude_code_agent_v1` | yes | no | no | yes | no | no | DG-CLAUDE-ADAPTER v1 (2026-08-24, C-1…C-6): `claude-code-v1` is a second approved provider in the reviewed registry (`app/coding_agents.py`), gated by `CLAUDE_CODE_AGENT_V1` (default off). Job-backed runner execution reuses the existing isolated-worktree path — instruction via file + stdin (INV-SSH-2), sentinel terminal contract (INV-SSH-6), no new validation mechanism. Engineering Task requests may name an explicit `agent_provider_id` from enabled approved providers only; default stays `codex`; no Auto selection; CLI version/protocol drift fails closed with no fallback; Claude credentials live only on the runner. Local suite `tests/test_claude_code_agent.py` is green. Enabled on the personal pilot 2026-08-25 (pilot checkout `ba1edeb`, `CLAUDE_CODE_AGENT_V1=true`) — personal-pilot deployment only; per DG-PERSONAL-PILOT-v1 pilot operation is not canary or production-readiness evidence, and no real-runner Claude CLI end-to-end task evidence is recorded in this repository. |
| `project_conversation_v1` | yes | no | no | yes | no | no | DG-CONVERSATION-V1 (2026-08-24, CV-1…CV-6, staged 2a): additive `ai_conversations`/`ai_conversation_messages` migration with SQLite as the only conversation truth (WS is transport); the 2a chat turn calls the Anthropic API through the existing LLM tool loop with zero tool expansion (INV-LLM-1/2/3 unchanged); conversations are pinned to one Project; every proposed action is its own approval card, never auto-chained; non-streaming "AI Engineer" project tab. The approved 2b successor was superseded by DG-AGENT-SESSION-V1. `PROJECT_CONVERSATION_V1_ENABLED` default off. `tests/test_project_conversation.py` is green. Enabled on the personal pilot 2026-08-25 (checkout `ba1edeb`, flag true) — personal-pilot deployment only; no canary or production-readiness claim. |
| `agent_session_v1` | yes | no | no | yes | no | no | DG-AGENT-SESSION-V1 (2026-08-24, D1–D6 + guardrails E-1…E-3): persistent AgentSession domain (additive `agent_sessions` migration, task-neutral schema carrying `provider_id`) behind the new never-auto-approved `agent_session_open` kind; per-turn bounded Claude Code channel as a named new validation mechanism (one tmux session + `exit_code` sentinel per turn, prompt via SFTP never shell strings, unreachable runner degrades without judging — INV-SSH-2/3/6/7-homomorphic; 10-minute turn timeout, 200 turns/session, 7-day idle close); persistent isolated workspace with dev-local tools only and zero platform tools in-session (tighter than the INV-LLM-1 ceiling); stream-json transcript with live tail; workspace diff endpoint; Development Session web workbench. `AGENT_SESSION_V1_ENABLED` default off. Suites `tests/test_agent_sessions.py`, `tests/test_agent_session_turns.py` are green; the UI pin moved from the retired `tests/test_agent_session_ui.py` into `tests/test_identity_workspace_v2.py::test_workspace_ai_engineer_tab_is_ported_faithfully_with_pinned_behaviors` (DG-UI-UNIFICATION v1 U6b/U8). Enabled on the personal pilot 2026-08-25 (checkout `ba1edeb`, flag true) — personal-pilot deployment only, and D3 confinement is explicitly approved for the non-production personal pilot only; no canary or production-readiness claim. |
| `agent_session_checkpoint` | yes | no | no | yes | no | no | DG-AGENT-SESSION-CHECKPOINT (2026-08-24, Option A): approval kind `agent_session_checkpoint` packages current session-workspace changes into a promotion candidate with real approval provenance; `engineering_task_promote` remains the separate human-only second gate — the two approvals are never merged and never auto-approved (`enqueue/stop` allowlist, INV-APPROVAL-4/4b, and DG-CODE-PROMOTE P-1…P-5 all unchanged). Reuses the existing path-policy double-check, bundle verification and promotion pipeline; bridge rows carry the checkpoint approval as `approval_id` with session-source metadata; Session UI shows the Promote action after checkpoint approval (request-side convenience only). `tests/test_agent_session_checkpoint.py` is green. Ships inside the agent-session surface on the personal pilot (same checkout and flag as `agent_session_v1`) — personal-pilot deployment only; no canary or production-readiness claim. |
| `experiment_v2` | no | no | no | no | no | no | DG-EXPERIMENT-V1 (2026-08-25, Option A, EX-1…EX-7) **approves** the bounded `experiment_create_v2` implementation: one matrix = one approval producing N fully-resolved `ExecutionPlanV2Spec`s; atomic all-or-nothing approve-time materialization preserving the existing one-plan-one-Job bounds (C6); batch-aware exclusivity without touching scheduler semantics; additive `experiments`/`experiment_plan_members` migration; `MAX_EXPERIMENT_RUNS=32`; read-only project-scoped listing/detail; `EXPERIMENT_V2_ENABLED` default off behind the `RUN_EXPERIENCE_V2_ENABLED` chain. Approved ≠ implemented: P1 has landed (pure contract module `app/experiment_v2.py`, additive migration 14 for `experiments`/`experiment_plan_members`, `experiment_create_v2` registered in `VALID_APPROVAL_KINDS` + `TRANSACTION_ONLY_APPROVAL_KINDS`, `EXPERIMENT_V2_ENABLED` default off; full suite 4354 passed) but is **inert until the P2 request/approve path exists** — no operable end-to-end surface yet, so `implemented` stays `no` until that lands with its recorded evidence. Not enabled anywhere, including the pilot. |
| `server_config_management` | yes | no | yes | unknown | unknown | no | All four public add/update/disable/delete request paths now pin exact canonical post-mutation YAML plus before/after digests and materialize through the immutable revision/journal protocol. A never-published legacy entry becomes revision 1 only through a fresh exact `server_update` human approval; migration does not backfill it, and retired/prepared history cannot be resurrected as legacy. Publication verifies the written digest before reload/activation; reload failure compensates only when the exact prior bytes are known, otherwise recovery stays fail-closed. Historical unpinned approvals remain honestly legacy, but their external YAML write now has durable hash-bound intent/applied/failed evidence without fabricating a revision. No deployment/canary evidence exists for these changes. |
| `mutable_dataset_registry` | yes | no | yes | unknown | unknown | no | Legacy `POST /datasets` directly persists a mutable registry row without an approval. |
| `immutable_dataset_snapshot` | yes | yes | no | no | no | no | `DG-DATASET-SNAPSHOT-v1` approved 2026-07-27. WP-3A now has a default-off local request→human approval→building→content-addressed publish workflow (`POST /datasets/{name}/{version}/snapshot-request`, `GET /dataset-snapshots*`) with atomic DB evidence, concurrent-winner fencing, source re-verification and approved-build resume. ExecutionPlan pins one published snapshot or explicit `dataset_none`; no production dataset/canary evidence exists. |
| `run_profile_v1` | yes | yes | no | no | no | no | Additive immutable revisions are consumed by ExecutionPlan: each plan pins an exact `run_profiles.id` revision and approve-time revalidation rejects missing/archived revisions. No production deployment evidence exists. |
| `auto_placement` | yes | no | no | unknown | unknown | no | Proposal and policy-scoped decision code exists behind default-off switches; no current deployment/canary evidence. |
| `backup_restore` | yes | no | no | unknown | no | no | Online backup refuses a missing database, reserves an exclusive destination, and creates per-artifact SHA-256 inventory pinned by `MANIFEST`. Restore verifies and safely stages the entire backup before displacement; traversal, absolute paths, links, devices and duplicate archive members fail closed. A disabled systemd oneshot/timer template requires explicit `BACKUP_ROOT`, while metrics report newest backup age with no unapproved threshold. `scripts/restore_drill.py` restores a copy, checks integrity/row counts and measures duration. No unit is installed/enabled, no off-host backup or real drill evidence exists, and numeric RPO/RTO/retention still need `DG-OPS-SLO`. |
| `node_protocol_v1` | yes | yes | no | no | no | no | Compatibility protocol remains available behind the protocol/drain flag; v2 request validation rejects agent-supplied `job_id`. Its compatibility claim now performs expiry/revalidation/reuse/insert in one `BEGIN IMMEDIATE`, while terminal→Job and artifact batches are each atomic. |
| `node_daemon` | yes | yes | no | no | no | no | `agent/__main__.py` is runnable. It fsyncs identity/digest/launch intent before effects and launches a separate durable supervisor that verifies `cmd.sh`, strips Node credentials from the workload environment, records boot-id/process-start identity and atomically fsyncs terminal evidence. The additive PR-10 isolation contract builds a detached user-systemd attempt unit with read-only control evidence, strict environment allowlisting, and bounded resource properties; direct supervisor mode remains the rollback path. A restarted daemon converges from that sentinel without owning the old process as a child. Stop uses identity-verified pidfd signaling and never trusts a bare PID; systemd restart preserves supervisors with `KillMode=process`. Reports remain durable/retryable and no listener is opened. Real activation still requires `DG-NODE-CANARY`. |
| `node_protocol_v2` | yes | yes | no | no | no | no | `DG-NODE-V2-v1` ruled 2026-07-28. Server-selected lease atomically creates linked generic/protocol attempts; DB partial unique indexes enforce one active attempt per Job/Node and one linked Node execution per server. Fresh FK/CHECK constraints plus additive migration guards reject future orphan/invalid rows. Ack projects the Job to running, stale heartbeat changes liveness only to unknown, and terminal converges both attempts plus the canonical Job while atomically appending exactly four completion operations. Node API bodies are bounded, forbid extras, and high-frequency synchronous DB work runs outside the event loop. Flags stay off, no node is enrolled, and real-machine activation needs `DG-NODE-CANARY`. |
| `execution_attempt_outbox` | yes | yes | no | no | no | no | Attempt claim + first prepare intent are one DB transaction; owner-only delivery covers pinned prepare/launch/stop/collect operations with an effect boundary. Exact companion/receipt/tmux/sentinel evidence settles an uncertain launch without replay, while a controller-won claim records definite non-transmission before requeue. Terminal attempts inherited from older code get evidence-only recovery without repeated hooks. Public generic-attempt stop and terminal collect are wired. Node terminal additionally creates an immutable exactly-four completion bundle (`dependency_refresh`, `result_collection`, `notification`, `owner_projection`) with fenced claim, durable outcome and restart recovery. Every rollout flag remains default-off; no production migration/canary proof exists. |
| `attempt_driven_ssh` | yes | yes | no | no | no | no | WP-2C runtime dispatch is wired into the scheduler with active-attempt exclusion, immutable revision identity checks, durable prepare/launch intents and D-5 revision-scoped filesystem evidence. Historical candidate `d73a38e...` passed the full `DG-WP2D-CANARY-v2` eight-hour window on `worker_5090_117` (20/20 collection, zero duplicate/false/lost/unknown/uncertain results, and all three drills); the committed summary is `docs/evidence/WP2D_V2_20260802_D73A38E.md`. Because later execution-path commits changed the candidate, the current branch is not promoted to canary-proven and must repeat the window. A fixed read-only SSH preflight records local/NFS/unknown on the exact active revision; scheduler selection and the DB claim transaction require `eligible`. SSH fallback remains the default. |
| `code_promotion_v1` | yes | no | no | no | no | no | Default-off public request/UI and manual approval path resolve only a terminal native Engineering Task with a verified canonical local-result bundle. Approval rechecks artifact bytes, performs real `git bundle verify` in staging, creates a non-runnable ProjectVersion, publishes an exact local Hub ref, then atomically marks it promoted. Interrupted publication is retryable; retirement preserves evidence. No GitHub push, worktree deletion, deployment or canary evidence exists. |
| `immutable_execution_plan` | yes | no | no | no | no | no | Ready plan and pinned pending approval are created in one SQLite transaction with immutable linkage. Approval materializes at most one target-pinned Job, and attempt creation revalidates plan/job/target digests. `GET /runs/{plan_id}` returns the durable plan→approval→Job→attempt→operation/event/node-artifact lineage without inferring failure from absent evidence. No deployment/canary evidence exists. |
| `codex_app_server_adapter` | yes | yes | no | no | no | no | Bounded fake protocol adapter exists; production wiring/version pin/canary remain separately gated. |
| `github_publication` | yes | yes | no | no | no | no | Interface and fake only; no production adapter or approved credential/egress configuration. |
| `workload_isolation_v1` | yes | yes | no | no | no | no | Local contract covers strict workload environment allowlisting, a detached user-systemd attempt unit, read-only control evidence, and bounded resource properties. Direct supervisor mode remains the rollback path; no unit is installed and no hostile-runtime evidence exists. |
| `worker_process_split` | yes | no | no | no | no | no | `PROCESS_ROLE=worker` and the non-HTTP `dispatch-worker` entry point own execution-shadow, durable lease/outbox, and result-recovery loops. Deployment, restart, and real worker evidence remain open. |
| `frontend_workflow` | yes | no | no | unknown | n/a | no | DG-UI-UNIFICATION v1 (U1–U8, 2026-08-25/26): the legacy `static/index.html`/`ui.js`/`ui.css` surface (Run Wizard/Approval Inbox/timeline/AI Engineer wizard/etc.) is retired and deleted; the single Chinese v2 Workspace (`static/workspace.html` + `workspace.js`/`workspace-features.js`) is now the sole UI surface, covering every migrated legacy panel plus ExecutionPlan Run cards, verified detail/timeline, safe Clone/Compare, approval-backed Stop state, and validated metadata-only artifacts with explicit loading/empty/error/legacy/unknown states. `GET /` always serves `workspace.html`, but the Workspace's own reads/writes all go through `/api/v2/*`, which stays behind `API_V2_ENABLED` (default off) — a clean configuration now renders only a minimal inline Chinese notice instead of a functioning UI, so `default-enabled` moves to `no`. Dependency-free static smoke gate (`scripts/frontend_smoke.py`) and JavaScript syntax check (`node --check`) cover only the two Workspace files now. No separate frontend deployment of this U8 change is claimed yet. |

2026-08-06 evidence note: the adoption gate now expands the closed dynamic
audit-writer families (`approval.kind`, server `action_name`, and
authorization-shadow `item.audit_action`) and requires every value to resolve
to the typed catalog. This strengthens inventory coverage without changing the
partial durable/legacy status or claiming external deployment evidence.

## Release blockers carried forward

| Blocker | Current evidence | Required destination |
|---|---|---|
| `RB-LAUNCH-001` | **Historical candidate `d73a38e...` passed the full WP-2D v2 evaluator.** The exact eight-hour window on `worker_5090_117` had 20 terminal attempts, 20 delivered collections, zero duplicate/false/lost/unknown/uncertain results, and all three drills passed. The evidence summary is committed at `docs/evidence/WP2D_V2_20260802_D73A38E.md`. Later execution-path commits mean the current branch is not automatically canary-proven. | Keep new assignment on the default-off/SSH-safe posture and repeat the full WP-2D v2 window for the current exact candidate before promoting rollout or closing the current release gate. |
| `RB-NODE-001` | v2 daemon/protocol, linked generic-attempt lifecycle, DB-enforced active ownership, durable supervisor/restart convergence, PID identity guard, atomic terminal/artifact writes, bounded API, stale-heartbeat unknown handling, exactly-four durable completion operations, staged rotation and drain/revoke split are implemented and locally tested; no real node evidence exists. | `DG-NODE-CANARY` plus Phase 5 (2 nodes / 100 jobs / 7 days); keep production workers on SSH. |

## Resolved blockers

| Blocker | Resolution evidence |
|---|---|
| `RB-DATASET-001` | `DG-DATASET-SNAPSHOT-v1` D-1 kept `POST /datasets` as an approval-free registry declaration and labelled it instead: `datasets.reproducible` is `0` for every row and no migration sets it to 1. D-5's rejection is now enforced on the request path — `POST /projects/{name}/runs/request` returns 400 `dataset_not_reproducible` and persists nothing, verified end-to-end through the real app in `tests/test_execution_plan_api.py`. The reproducible alternative (`dataset_snapshot`) exists alongside. |
| `RB-SERVER-001` | All four approved server mutations publish through the protocol; unpinned historical approvals stay honestly `legacy_observed`. A never-published legacy target needs a fresh exact update approval to create revision 1; no history is fabricated. Digest drift, failed writes, reload failure and an unknown disk state each have an explicit outcome, and the operator recovery surface cannot fabricate a revision or edit a digest. Current full offline suite: 3,851 passed. |
| `RB-STOP-001` | WP-1C pins new stop approvals, persists an immutable legacy intent before delivery, never projects kill success/failure to terminal, blocks requeue/re-dispatch while unresolved, and closes only with terminal evidence. Stop failure, scheduler crash/requeue, and terminal convergence tests pass in the current 3,851-test full suite. |

## Updating this ledger

Every work package must update its affected rows and blockers. A field moves to
`yes` only with direct evidence recorded in `docs/DECISIONS.md`, `docs/evidence/`,
or the PR that changes the row.
Missing deployment/canary evidence stays `unknown` or `no`; it is never inferred
from passing unit tests.
