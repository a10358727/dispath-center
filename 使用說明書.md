# AI 訓練調度中心 — 使用說明書

日常安裝、啟動、工作節點接入與操作手冊。深入 API、備份與各階段驗收步驟
見 `README.md`。

> 範例使用 `.env.example` 的預設 listener `127.0.0.1:8000`。若你修改
> `API_HOST`／`API_PORT`，請同步替換網址；vLLM 必須使用另一個 port。

---

## 1. 這是什麼

一個跑在 Server A 上的訓練任務調度中心：

```
你（網頁 / 對話 / curl）
   ↓ 建立 approval record
核准（依設定由網頁一步生效／預定規則／人工點擊）
   ↓
排程器（每 10 秒）：挑空閒機器 → SSH 派工（tmux + 哨兵檔案）
   ↓
工作機跑任務 → 完成後自動：收結果回 Server A → 寄 Email → 寫稽核
```

鐵律：所有會「做事」的 material 動作都要經過 approval record 與
稽核。但這不代表每次都需要第二次人工點擊：預設
`WEB_DIRECT_EXECUTE=true` 會讓網頁發起的派工／停止在同一個請求內經既有
`approve()` 落地，而使用者預先配置的自動核准規則也只可核准
enqueue/stop。伺服器設定等其他 material mutation 仍需待審核准。含
`rm -rf`、`dd`、`shutdown` 等黑名單字樣的指令直接拒絕、不給核准機會；
每個動作都寫進 `audit.jsonl`。

---

## 2. 啟動與停止

### 一鍵啟動（推薦給第一次使用與本機開發）

```bash
cd ~/dispath-center
./quickstart.sh
```

第一次執行會自動：

1. 缺少 `.env` 時，從 `.env.example` 建立 mode `600` 的 `.env`。
2. 缺少 `servers.yaml` 時，建立只含 `servers: []` 的 mode `600` 檔案，
   不探測示例 IP。
3. 建立 `.venv`，只在 `requirements.txt` 內容變更時安裝依賴。首次安裝
   需要能連到已設定的 Python package index（或本機有完整快取），不是
   離線安裝包。腳本會自動略過缺少 pidfd 支援的 Conda Python，改用符合
   需求的系統 Python 建立專案專用環境。Debian／Ubuntu 若無法建立 venv，
   先安裝 `python3-venv`；如需固定版本，可用
   `PYTHON_BIN=/path/to/python ./quickstart.sh` 明確指定。
4. 驗證設定；只允許 loopback、RFC1918、link-local、Tailscale
   `100.64.0.0/10` 或對應的私有 IPv6 位址。public／unspecified 位址一律
   拒絕；沒有 `AUTH_TOKEN` 且 OIDC 關閉時更只允許 loopback。
5. 背景啟動本體、等待 `GET /` ready，再顯示網址。

它不會自動啟動 MCP bridge、cloudflared、Node Agent 或任何 worker process。
新建的空 `servers.yaml` 不會探測主機；如果檔案原本已有 enabled／disabled
節點，`app.main` 的既有 monitor 仍會啟動並照設定發出唯讀 SSH probe。

常用命令：

```bash
./quickstart.sh status
./quickstart.sh logs --follow
./quickstart.sh restart
./quickstart.sh stop
./quickstart.sh foreground      # 前景跑，Ctrl+C 停止
./quickstart.sh setup           # 只準備環境，不啟動
```

PID、Linux boot ID、process start time、endpoint 與 log 放在 gitignored 的
`.runtime/dispatch-center/`，目錄／檔案權限為 `700`／`600`。腳本只會以
pidfd 對「boot ID、PID、start time、cwd、argv 都符合本專案 venv launcher」
的 `.venv/bin/python -m app.main` 行程送 signal，絕不接管或停止占用同一
port 的其他程式。

`quickstart.sh` 是單機開發管理器。不要讓它、舊的 tmux `start.sh` 與 systemd
同時管理同一個 listener。

系統需求是 Linux、可用的 `/proc`、pidfd（主線 kernel 5.3+）、GNU
coreutils 與 Python 3.10+；腳本會在 spawn 前 fail closed 檢查。前景模式是
以 `exec` 交給 app；Ctrl+C 後若留下 stale metadata，下一次 `start`／`stop`
會用 boot ID + start time 確認原程序已不存在後安全清除。

### 手動前景執行

```bash
cd ~/dispath-center
source .venv/bin/activate
python -m app.main          # 前景執行，Ctrl+C 停止
```

### systemd 常駐（正式使用，開機自啟、當掉自動重啟）

```bash
sudo cp deploy/dispatch-center.service /etc/systemd/system/
sudo nano /etc/systemd/system/dispatch-center.service   # 改四處 YOUR_USER 佔位
sudo systemctl daemon-reload
sudo systemctl enable --now dispatch-center

journalctl -u dispatch-center -f     # 看 log
sudo systemctl restart dispatch-center   # 改了 .env / servers.yaml 之後重啟生效
```

正式環境可先執行 `./quickstart.sh setup` 準備 venv／設定；在執行 setup 前，
必須先停止任何共用同一 venv 的 systemd／舊 `start.sh`／手動程序；若先前曾
用 quickstart 背景版啟動，則先執行 `./quickstart.sh stop`。確認 `.env` 已啟用
認證後才交給 systemd。不要由腳本自動修改 `/etc` 或使用 root 執行應用。

重啟不會弄丟任務：執行中的任務靠哨兵協議在重啟後自動對帳
（exit_code 檔案在就判成敗、tmux 還在就繼續等、都沒有就重新排隊）。

---

## 3. 開網頁

- Server A 本機：`http://127.0.0.1:8000/`
- 從自己的電腦（二選一）：
  - `.env` 的 `API_HOST` 改成 Server A 的 Tailscale IP，重啟服務後開
    `http://<Tailscale IP>:8000/`（只准私網 IP，不要 0.0.0.0）
  - 或 SSH 隧道：`ssh -L 8000:127.0.0.1:8000 你@ServerA`，開
    `http://127.0.0.1:8000/`

頁面上方會顯示「目前使用者」。如果已啟用 OIDC 且尚未登入，按
「登入」會往公司 IdP 完成 Authorization Code + PKCE 流程，回來後以
可撤銷的 server-side session 使用。按「登出」會撤銷當前 session。

舊的 `.env` `AUTH_TOKEN` 可在 OIDC 啟用期間同時保留作回退：遇到 401 時
輸入一次，瀏覽器會記在 `localStorage`。這是 legacy 相容機制，不是新主要
登入方式；OIDC rollout 完成後應清掉瀏覽器不再需要的 localStorage token，
server 端回退路徑可繼續保留。`AUTH_TOKEN` 未設且 `OIDC_ENABLED=false` 才是
open-development 模式。

### OIDC 登入注意事項

- 瀏覽器必須以 HTTPS 開 Dispatch Center；OIDC cookie 固定為
  `Secure` + `HttpOnly` + `SameSite=Lax`，純 HTTP 不會建立可用 session。
- 登入前的頁面位置會作為 `return_to`，但只接受同來源、以 `/`
  開頭的相對位置；絕對 URL 或 `//其他網域` 會被拒絕。
- Actor 只以 IdP 給的精確 `(issuer, subject)` 辨識；email 只是顯示
  metadata，改 email 不會換身分，同 email 也不會合併帳號。
- 最初的 platform admin 必須在第一次登入前，用 IdP 的精確
  `sub` 設在 `OIDC_PLATFORM_ADMIN_SUBJECTS`。第一個登入的人不會自動成為
  admin，既有 actor 後來才加進 allowlist 也不會被提升。
- OIDC 是認證，不是 authorization enforcement。`AUTHORIZATION_MODE=shadow`
  只記錄 would-deny，不會實際擋住專案讀寫。

完整的 IdP 註冊、`.env` 設定、反向代理 callback log 防洩漏與回退
步驟見 `README.md` 的「2.2.1 OIDC 瀏覽器登入」。

### 四個分頁

| 分頁 | 用途 |
|---|---|
| 總覽 | 每台機器卡片（在線/離線、GPU%、磁碟、已快取資料集）＋最近事件流＋待核准卡片 |
| 專案 | 專案列表、「派工」按鈕（彈窗選機器，標示 免同步✓／需同步 N GB／忙碌將排隊／自動） |
| 任務 | 佇列表格；點開看 log 尾；排隊中可取消；執行中可請求停止；失敗的可「診斷」「重跑」；卡黃=疑似卡死 |
| 對話 | 聊天派工（vLLM agent）；核准卡片直接在對話裡按核准/拒絕 |

---

## 4. 日常流程 A：丟一個一次性指令

1. 任務分頁（或對話分頁打「跑 nvidia-smi」）建立請求。curl 版：
   ```bash
   curl -s -X POST http://127.0.0.1:8000/dispatch \
     -H "X-Auth-Token: 你的token" -H "Content-Type: application/json" \
     -d '{"command": "cd projects/foo && python train.py"}'
   ```
2. 總覽頁出現待核准卡片 → 按「核准」。
3. 排程器 10 秒內挑空閒機器派出去；任務分頁看狀態與 log 尾。
4. 跑完：自動收 `results/{id}/` 回 Server A、寄信（有設 SMTP 的話）。

可選欄位：`pin_server`（指定機器）、`require_tag`（如 `"gpu"`）、
`priority`（`normal`/`low`，low 是待辦池，閒了才跑）。

## 5. 日常流程 B：專案 + 大資料集訓練

只需做一次的準備：

```bash
# 1) 註冊資料集（掃描 Server A 上的目錄，產生 manifest）
curl -s -X POST http://127.0.0.1:8000/datasets \
  -H "X-Auth-Token: 你的token" -H "Content-Type: application/json" \
  -d '{"name": "defect", "version": "v3", "source_path": "/data/defect-v3"}'

# 2) 建專案（掛上資料集；setup_cmd 第一次上某台機器時自動跑）
curl -s -X POST http://127.0.0.1:8000/projects \
  -H "X-Auth-Token: 你的token" -H "Content-Type: application/json" \
  -d '{"name": "resnet50", "repo_or_path": "git@github.com:you/resnet50.git",
       "dataset_name": "defect", "dataset_version": "v3",
       "default_command": "cd projects/resnet50 && python train.py",
       "setup_cmd": "pip install -r requirements.txt"}'
```

之後每次訓練：專案分頁 →「派工」→ 彈窗選機器：

- 機器旁顯示「免同步 ✓」→ 直接跑。
- 顯示「需同步 N GB」→ 核准卡片會帶完整計畫：先 sync（rsync 推過去、
  驗證檔案數/大小、登記快取）再自動跑訓練，一次核准全包。
- 選「自動」→ 只會派給**已有資料**的機器；資料哪台都沒有時會一直排隊
  （卡片上會警告）。想把資料帶上新機器，請用指定機器派工。

## 6. 對話 / Agent

網頁「對話」分頁在 `.env` 同時設定 `VLLM_BASE_URL` 與
`VLLM_MODEL` 時，會使用你實際設定的模型走本地 vLLM agent；專案沒有
內建或強制的預設模型。沒設 vLLM 時，WS 會沿用 Anthropic／規則式後備，
`POST /agent/chat` 則回 503。啟用 vLLM 後可直接打中文，例如「現在哪台
機器有空？」「幫我把 resnet50 派去 server-b 跑」——它會查狀態、
建立核准請求（灰色小字顯示它呼叫了哪些工具）。它**只能查詢
和建立待核准請求**，沒有任何工具可以核准、執行指令或碰檔案；
是否由使用者預先寫好的規則自動核准，是 approval layer 另行決定。

腳本用的固定端點（不經 LLM）：

以下範例明確使用 legacy `X-Auth-Token`；也可以改用已啟用的 service
`Authorization: Bearer ...`。命令列 `curl` 不會自動帶瀏覽器的 OIDC session
cookie；open-development mode 才能省略 credential header。

```bash
# 快速查詢：status / servers / jobs / approvals / events / gpu / vllm
curl -s -X POST http://127.0.0.1:8000/agent/cmd \
  -H "X-Auth-Token: 你的token" -H "Content-Type: application/json" \
  -d '{"cmd": "gpu"}'

# 完整 agent 對話（REST 版）
curl -s -X POST http://127.0.0.1:8000/agent/chat \
  -H "X-Auth-Token: 你的token" -H "Content-Type: application/json" \
  -d '{"text": "有失敗的任務嗎？失敗原因是什麼？"}'
```

vLLM 沒開時：網頁 WS 有 `ANTHROPIC_API_KEY` 就沿用 Anthropic，
否則退回規則式（「狀態」「任務」「跑 <指令>」三種用法）；
`POST /agent/chat` 回 503，系統其他功能不受影響。

### AI 工程任務（ProjectVersion-pinned 相容後端）

新版 wizard 預設可用，但後端 flag 預設關閉；關閉時它仍用既有
`/coding-task-request` 建 pending approval，版本只供參考。要啟用 immutable
base，先確認專案已做 Hub sync、版本頁已有 `ProjectVersion`，再於 `.env` 設：

> **目前不要在 operational 環境執行下列啟用／重啟步驟。** 現行 worktree
> 尚缺 post-agent Git finalization 的 no-network sandbox、trusted Git metadata
> 與 hard resource limits；只停用 Git hooks/external diff 並不足以關閉
> clean/process/smudge filter 與資源耗盡風險。須先完成並核准
> `docs/AI_ENGINEERING_DECISION_GATE.md` D2。以下只保留作日後 gate 完成後的
> 操作流程，不代表現在已可部署。

```bash
ENGINEERING_TASK_BACKEND_V1=true
```

`.env` 只在 process 啟動時讀取，因此這一步需要重啟調度中心：

```bash
./quickstart.sh restart
# 或 systemd 環境：sudo systemctl restart dispatch-center
```

不必重啟 worker，也不必手動執行 migration。回到網頁重新整理後，在專案卡
選「建立 AI 工程任務」：

1. 先確認 Runner 顯示已設定、online、Codex 已安裝且已登入；busy 仍可送出，
   核准後會排隊。
2. 選一筆 ProjectVersion。新版送出後 execution preview 應顯示 exact commit，
   不再顯示「執行時解析 HEAD」。
3. 填 objective、背景、預期變更、驗收與 validation 偏好。
   `allowed_paths` 是必填的 machine-readable 修改範圍：`.` 代表整個
   repository、結尾 `/` 代表 subtree，無結尾 `/` 則只允許 exact path。
   `prohibited_paths` 是獨立的 machine-readable deny list，與 allow 重疊時
   永遠優先；`prohibited_changes` 只是給 agent 與核准者看的自然語言
   要求，不是技術強制的 path policy。dependency install、external
   network、raw environment/secret 仍不能從此表單授權。
4. 送出只建立 `coding_task` pending approval。人工核准後，Server A 的
   `_local` staging Job 會把四個輸入傳到 Runner：exact Hub bundle、預先寫好的
   instruction、canonical `path-policy.json` 與 exact verifier source。policy 與
   verifier 的 SHA-256 已綁定在 immutable approval contract；path rules 是
   data file，不會插入 shell command。Coding Job 必須等 staging 成功才會啟動。
5. Agent 結束後，Runner 先檢查 approved branch/base ancestry，將最終 tree
   整理成 approved base 後最多一個 result commit，再檢查 final Git object
   mode、秘密檔名、`allowed_paths` 與 `prohibited_paths`。外層 Runner 不會直接
   執行 agent 可修改的 pytest/repository code；產生 bundle 前只會再次核對
   ref 與政策。越界時不產生可接受的 diff/bundle，
   task 顯示 `path_policy_violation`；秘密檔名違規則顯示
   `secret_violation`，固定錯誤文字不顯示違規路徑。
6. Server A 不相信 Runner 回報的 `done`；收件時會將 bounded regular-file
   bundle 複製到 private bare repository，獨立重驗 pinned base/result ancestry、
   approved policy 與 exact verifier。不通過時會清除 result/bundle pointer 並
   拒絕對應 artifact，不會因 Runner 曾報 `done` 就接受。
7. 可從「AI 工程任務」看 structured task，也能看到舊 Coding Run；舊資料會
   明確標為 `legacy_unpinned`，系統不會猜測它原本使用哪個 ProjectVersion。

如果表單要求 tests/lint，Codex 可在自己的 sandbox 回合內執行；回合結束後
Runner 不會另開一個無 sandbox 的 `python3 -m pytest`。在後續受控 validation
sandbox 尚未完成前，task 的結構化 test 結果會顯示「未執行」。要取得獨立
驗證證據，請使用下方核准式 Worker validation。

這項 path policy 只保證「最終 Git 結果」，不是 agent turn 期間的 live
filesystem confinement；曾被修改後又復原、未出現在 final tree 的檔案不在
此保證內。現行 provider 也沒有 inner-command approval callback。
`GET /coding-agents` 的 runtime metadata 會如實顯示
`inner_command_approval=unavailable`、`inner_command_enforcement=sandbox_only`、
`turn_time_path_confinement=unavailable`，以及
`final_git_path_policy=runner_pre_bundle_and_server_a_pre_accept`。

任務詳情的「下載去敏後的已收集 patch」只會在伺服器回傳
`available_actions.download_patch.enabled=true` 與該 task UUID 的精確
same-origin URL 時開啟。它會沿用目前 OIDC session 或 legacy token，整份
回應先收到 browser memory；檔名只用 task UUID 與伺服器回傳的
redacted 狀態，不會使用專案、標題或使用者文字。它是 bounded、sanitized
的 collected patch，不是 raw artifact，也不代表 verified 或 canonical patch。
原始 bundle 可能含未去敏內容，所以 Download bundle 永久停用，平台不提供
raw bundle download 端點。

Task detail 會把 attempt、進度事件、命令狀態、遮罩後 log、diff/test、artifact
metadata 與核准歷史分頁顯示。`failed`、`interrupted`、`disconnected` 與
`unknown` 是不同狀態；Runner probe 失敗也不會被寫成「Codex 未安裝」。新任務
內部 Job 的 executor command、Server A path 與 raw log 不會從一般 Jobs、
Coding Runs、聊天或 MCP 相容入口顯示。

終態 Engineering Task／Worker validation log 在寫進 SQLite 前就會遮罩
credential 與 private path；compatibility Coding Job 也適用，遠端 tail 先受
64 KiB byte cap，遇到 private-key marker 時整段不落庫。Native artifact 必須
有 canonical row、完整 SHA-256/size 並與目前 descriptor 精確相符；缺少、漂移、
rejected/withheld 都不會由其他詳情 API 重新讀出。`Authorization: Basic` 與含
密碼的 HTTP/SSH URI userinfo 會在 native／compatibility request 持久化前拒絕
並在輸出遮罩；不含密碼的 `ssh://git@host` 不受影響。

平台在派發、running reconcile、stall probe 與 stop 前，還會核對核准時記錄的
Job owner、approval、command role 與 command SHA-256；若資料庫中的 Job command
後來被改動，只會顯示 `execution_contract_mismatch`，不會把被改動的命令送到
Runner。Provider 清單目前也只接受平台 allowlist 內的 Codex
`codex-exec-v1`。它已透過 `CodingAgentProvider` 產生既有單次 turn 的固定 launch，
但不是 app-server runtime：`GET /coding-agents` 會如實顯示 start/final response
可用，而 resume、task-safe cancel、checkpoint、live event stream 與逐命令核准
都不可用。這些操作會拒絕，不會改走任意 shell 或另一個 executor；
外層 final-Git 守門也不能當成逐命令核准。

Immutable AI 工程任務的 external network 仍固定 disabled；只有 legacy Coding
Task 可能沿用 Platform Admin 既有的 `CODEX_NETWORK_ACCESS` 設定。兩者不能互相
推導授權，dependency installation 也仍未被核准。

請不要從一般任務頁取消、重跑或診斷 AI 工程任務的 staging/coding Job；介面會
導回 AI 工程任務詳情，這些 task-safe action 要等後續受控執行切片。若 Job 已
running，停止仍照舊建立 `stop` approval。服務重啟後若要補收結果，平台只會
連回與核准時 `name/host/user/port` 完全相同且仍啟用的 Coding Runner。若結果
已收妥、只有 artifact metadata 暫時寫入失敗，下一輪 recovery 只補 metadata，
不會再次執行 Codex 或重拉已固定的結果。

若需 rollback，把 flag 改回 `false` 並只重啟調度中心；既有 DB rows、legacy
Coding Task、scheduler、SSH worker 不需回滾或重啟。已核准/執行中的 Job 仍按
既有哨兵流程對帳，不能用關 flag 當成停止任務；要停止 running Job 仍走 stop
approval。

#### 從 immutable task 申請 Worker 驗證

當 native AI 工程任務已完成、verified bundle 已安全收回，而且 task detail
顯示「後續驗證」可用時：

1. 開啟任務詳情的驗證動作，選擇一台明確的 enabled Worker；不能選自動派位或
   `_local`。
2. 填入一個 bounded validation command，以及需要的 tag、priority、GPU 偏好。
3. 送出後只會看到 `kind=enqueue` 的 pending approval；此時尚未建立 Job，也
   尚未連線 Worker。
4. 核准頁檢查 ProjectVersion/base、原始 task approval、target/project instance、
   bundle hash/size 與 push/validation command digest。核准時若任何 material
   evidence 漂移，系統會拒絕而不連線。
5. 核准成功後才建立 ordinary sync Job 與 dependent Worker Job，可在任務詳情或
   一般 Jobs 看安全狀態。頁面不會顯示 executor command、私鑰路徑或 raw log。

queued validation Job 可沿用普通取消。若已 running，停止仍要建立既有 `stop`
approval；stop 核准前也會重驗合約。`unknown`、`offline`、`disconnected` 表示目前
無法證明遠端狀態，不等於 validation failed。

舊 Coding Run 仍使用原有 `/dispatch`「後續驗證」流程；平台不會替它補造
ProjectVersion。這個版本也不會讓 Codex 自動要求 Worker、多輪執行 validation、
建立 Run Profile、promote、發 PR 或部署。

### 專案工作區（bounded Slice 6）

從「專案」清單點入專案後，現在以固定子導覽分成七區：概覽、程式碼與版本、
AI 工程、執行與驗證、資料與產物、設定、部署。原本的 bookmark
`#project/<encoded-name>` 仍可使用，等同開啟概覽；也可以直接收藏：

```text
#project/<encoded-name>/overview
#project/<encoded-name>/code-version
#project/<encoded-name>/ai-engineering
#project/<encoded-name>/runs-validation
#project/<encoded-name>/data-artifacts
#project/<encoded-name>/settings
#project/<encoded-name>/deployment
```

section 拼錯或來自舊連結時會回到概覽，不會建立新 API request。各區只是把既有
功能放回清楚的位置：AI 修改與普通 Runtime Job 分開、activity 探測留在執行與
驗證、刪除放在設定危險區、deployment 則獨立顯示為高風險操作。

專案名稱旁的角色標籤只顯示目前專案的 membership（或 Platform Admin／service
actor 說明）。它不會因 Viewer、Operator、Project Admin 等角色而在瀏覽器隱藏
或啟用按鈕；真正能否執行仍由既有 server-side authentication、approval 與
`AUTHORIZATION_MODE=off|shadow` 決定。

請注意這個切片的限制：

- 「程式碼與版本」會顯示現有 Hub、ProjectVersion 與 instances，但普通 Runtime
  Job 尚未固定 immutable ProjectVersion；AI 工程任務的 pinned backend 仍依前節
  的 `ENGINEERING_TASK_BACKEND_V1` 決定。
- 「執行與驗證」尚未提供 Run Profiles。
- 「資料與產物」尚未提供跨整個專案的 artifact aggregation、統一 manifest 或
  run comparison。
- 「部署」只沿用既有 deployment approval，不會自動 promote、merge 或 deploy。
- Runner/activity 顯示 disconnected、unknown 或暫時無資料時，不代表任務、Job
  或 instance failed；不要因畫面暫時無法探測就手動改寫遠端狀態。

這次 UI 整理沒有 database migration，也沒有改 scheduler、SSH 或 worker。部署
後不需手動 migration，也不需重啟 worker 或 Coding Runner。這不代表其他平台
頁面、Run Profiles、publication 或整份 UI/UX 計畫都已完成。

### Supporting surfaces（bounded frontend-only Slice 8）

現有的 top-level hash routes 不變；以下頁面只是在原 route 內多一層
semantic subnav：

- `#tab/overview`：Health、核准、活動與稽核、身分與管理摘要。
- `#tab/servers`：Coding Runner、Worker servers、Inventory。
- `#tab/datasets`：Datasets、Results 與 Artifacts 能力邊界。
- `#tab/jobs`：Custom command、未來 Run Profiles 說明、任務佇列。

這些按鈕只在當前頁面切換 section，不會建立新的 backend request。
Projects、AI 工程任務、助手等原有 route 也保持不變。

核准區的文案需特別注意：

- 「待核准」是目前已認證介面可見的**全平台 pending 清單**，不是
  「待我核准」。現在後端還沒有回傳可供前端使用的 per-action capability。
- 「我提出的」只在 approval 的 `requester_actor_id` 與目前
  `/auth/me` actor ID 完全相同時列入；不用 display name、email 或 alias
  推測。
- `requester_actor_id=null` 或沒有記錄的 legacy approval 仍顯示為提出者
  unknown，不會被歸給目前使用者。
- 展開「完整 immutable payload」可核對實際核准內容。新版 AI 工程任務會顯示
  ProjectVersion 與 exact base commit，不會寫成「目前 HEAD」。
- `service_token_issue` 的 secret 只會在核准 response 出現一次；目前通用網頁與
  chat 不會安全接收它，所以只提供拒絕，不提供核准。請使用能安全接收一次性
  response 的管理 client。

Coding Runner status 不再跟著全局每 5 秒 `refreshAll()` 探測。
`GET /codex-runner/status` 在 backend cache miss 時可能執行唯讀 SSH，
cache 為 30 秒；現在只會在切到 Infrastructure 內的 Coding Runner subsection、
手動按「重新檢查」／「重試」，或開啟 legacy Coding Task modal 時由
supporting UI 觸發；停留在預設 Worker servers subsection 不會探測。
AI 工程任務 wizard 本身開啟時仍會檢查 availability。因此頁面上的
Runner status 不是高頻即時 telemetry；disconnected、unknown 或暫時無資料不代表
Coding Task 或 Job failed。

背景更新中若只有某一個 API 失敗，只有對應區域顯示「無法更新」與重試；其他
區域仍會更新。這個狀態不代表 Job、Runner 或 approval 本身 failed。

「Results 與 Artifacts」目前只提供現有 Job、AI task evidence 的入口，
不會合併猜測 lineage、hash 或關聯。Run Profiles 只是 future capability
說明，還沒有 schema、API 或 persistence。「身分與管理」也只顯示
actor、membership 與 scope 摘要，不提供 service account、token 或 membership
mutation UI；這些後端能力仍由 feature flag 與 approval 保護。

這個 Slice 8 沒有 database migration，也沒有改 authorization、approval、
scheduler、SSH 或 worker。它不代表 global result lifecycle、Run Profiles、完整
administration UI、publication 或整份平台計畫已完成。

## 7. 停止、重跑、診斷

- **停止**：任務分頁對執行中任務按「停止」→ 建立 stop approval。
  預設 web-direct 會在同一請求核准；關閉它時才會留在 pending，等人工
  核准後 kill。任務標為 cancelled。
- **重跑**：對結束的任務按「重跑」→ 複製原參數建新的 enqueue
  approval；是否同請求核准同樣受 web-direct 設定影響。
- **診斷**：失敗任務按「診斷」→ 把 log 尾＋專案結構餵給你設定的
  模型，給出原因分析與 diff 建議。**只是建議，絕不自動改碼或
  重跑**；分析品質取決於實際模型，請當作第一眼參考。
- **疑似卡死**（列黃）：log 超過 30 分鐘（`STALL_MINUTES`）沒增長。
  只是提醒，不會動任務——自己看 log 判斷是慢還是真的掛了。

## 8. 架設受控工作機（目前正式支援：SSH worker）

### 8.1 先釐清：目前沒有可安裝的 Node Agent

目前唯一實作並受保護的 worker transport 是 Server A 主動發起的
SSH／SFTP／tmux。Repository 裡沒有 Node Agent daemon、安裝套件、systemd
unit、poll/lease/heartbeat 協議或 node credential：

- `app/agent_runtime.py` 是 Server A 上的 vLLM 對話 tool loop，不是 worker agent。
- worker 上的 `agent_jobs/{id}/` 是任務腳本與哨兵目錄，不是常駐 agent。
- Central Codex Runner 也仍由 SSH 控制。

正典 `INV-SSH-1` 目前明定工作機不安裝本系統常駐程式，因此不能把不存在的
Node Agent 當成已可部署。下面是現在可以正式使用的 SSH worker 架設流程。

### 8.2 工作機準備（Debian／Ubuntu 範例）

在工作機建立非 root 帳號。一般派工需要 SSH server、`bash`、`tmux`；
下例也安裝 `rsync`，因為資料集 sync、結果回收與 bundle 傳輸的
rsync-over-SSH 會在兩端呼叫 `rsync` 可執行檔。這些傳輸由 Server A 主動
發起，不需要遠端 rsync daemon，也不是安裝本系統的常駐 agent：

```bash
sudo adduser train                         # 已有一般帳號可跳過
sudo apt-get update
sudo apt-get install --yes openssh-server bash tmux rsync
sudo systemctl enable --now ssh
sudo -u train mkdir -p /home/train/projects /home/train/datasets
```

GPU 機還必須由既有 driver 提供可執行的 `nvidia-smi`。不要為 Dispatch Center
在工作機安裝 Python daemon、curl/jq agent 或開新的控制 port。工作機只需讓
Server A 經私網 SSH 進入；建議使用 Tailscale IP／私網 hostname。

### 8.3 在 Server A 建立專用 SSH key

推薦使用互動式 onboarding helper（工作機必須已存在非 root 帳號並啟用 SSH）：

```bash
cd /home/formosa/dispath-center-goal1-oidc
./onboard-worker.sh \
  --host 100.64.0.21 \
  --user train \
  --name worker-gpu-01 \
  --gpu
```

第一次執行會建立 `~/.ssh/dispatch_worker`，接著由系統的 `ssh-copy-id` 在
終端機直接詢問遠端帳號密碼。密碼不會傳給 Dispatch Center、不會出現在
命令列參數、環境變數、檔案或 log。公鑰安裝後，腳本用 key-only SSH 唯讀
檢查 `bash`、`tmux`、`rsync`（`--gpu` 再檢查 `nvidia-smi`），並印出可貼到
網站的 disabled 設定。它不會修改 `servers.yaml`、建立 membership、啟用
工作機、派工或安裝 Node Agent；正式加入仍依 §8.4 走網站核准流程。

這個 helper 與應用現況相同，沿用不驗證 host key 的 Tailscale 私網取捨；
只應對可信的 Tailscale／私網位址執行，不可拿來 bootstrap 不受信任的公網
主機。

**若遠端缺少 tmux／rsync／git／Python venv（Goal 3 Phase B，預設關閉，
`SERVER_BOOTSTRAP_V1_ENABLED=true` 才啟用）**：金鑰配置完成後不必再手動
`apt-get install`——helper 會印出一段 `curl -X POST
/servers/bootstrap-request` 指令，核准後平台以審閱過、SHA-256 已釘選的
固定腳本在**非 root**層級補齊這幾樣使用者層工具並留下報告
（`GET /servers/bootstrap-reports`）。**系統套件與 GPU 驅動不在清單內，
平台永遠不會自行提權安裝**——這些仍要依 §8.2 由管理者以 root 手動處理。
報告未通過時，「新增伺服器」的核准會直接被拒（見 §8.4）。舊路徑（手動
`apt-get install` 齊全再重跑 helper）仍然有效，兩條路都會走到同一個
§8.4 disabled 登記流程。

**Node Agent（Goal 3 C2，預設關閉，目前還不會接手派工）**：這是未來讓
工作機主動回報狀態的新通道。設 `NODE_AGENT_V1_ENABLED=true` 之後，可以在
核准頁替某台機器登錄一個 agent 身分——**憑證只會在核准的那一次回應裡出現
一次**（平台只保存雜湊，遺失就只能撤銷後重新登錄），要把它填進那台機器上
agent 的設定。撤銷是一台一台來的，撤掉一台不影響其他台。

要真的把某台機器改成走 agent，在 `servers.yaml` 那台底下加
`execution_backend: node`（預設是 `ssh`，不寫就是 SSH）。**這是一台一台
設定的**——改一台不影響其他台;要退回只要把欄位改回 `ssh`（或刪掉），
下一輪排程就恢復 SSH 派工，不必搬任何資料。旗標關閉或值打錯一律自動當成
`ssh`，「不確定就走已知可用的路」是刻意的設計。總覽頁的 `GET /servers`
會顯示每台機器**現在實際**走哪條通道。

另外有一道保護：只要某個 agent 已經領走（或確認接手）某個任務，排程器就
不會再從 SSH 派同一份工作——避免同一個訓練不小心跑兩份。

⚠️ **但先別急著切**：C3 規定要先做完實機驗證（至少 100 個任務、至少 2 台
機器、連續 7 天沒有重複啟動也沒有假失敗）才算過關,而這個驗證還沒做。
現在把機器改成 `node` 等於跳過驗證。不想碰的話保持預設就好,什麼都不會變。

**新機開通後的資料集預熱（Goal 3 B4，預設關閉）**：機器剛加進來時本地
沒有任何資料集，第一個要用到資料的訓練任務得先等一次 sync。把
`DATASET_PREWARM_V1_ENABLED=true` 與 `DATASET_PREWARM_KILL_SWITCH=false`
**兩個都設好**（少設一個就完全不作用，這是刻意的雙重保險）之後，平台會
定期檢查「已啟用、但一份資料集都還沒有」的機器，替它挑一個目前最多其他
機器都有的資料集（大小相同時挑小的），在核准頁產生一張
`dataset_prewarm` 提案卡片。**這只是提案，平台不會自己動手**——你點核准
之後才會建立 sync 任務，而且走的是跟你自己指定機器派工時完全相同的同步
流程。每台機器每輪最多一張卡片；同一組（機器＋資料集＋版本）不論你核准
還是拒絕，`DATASET_PREWARM_COOLDOWN_SEC`（預設一小時）內都不會再問第二
次。不想要這個功能就保持預設不動即可。

等價的手動流程如下：

```bash
install -d -m 700 ~/.ssh
ssh-keygen -t ed25519 -a 64 -f ~/.ssh/dispatch_worker -C dispatch-center
ssh-copy-id -i ~/.ssh/dispatch_worker.pub train@100.64.0.21
chmod 600 ~/.ssh/dispatch_worker
```

私鑰只留在 Server A，`servers.yaml`／網頁只填私鑰「路徑」，絕不貼私鑰內容，
也不能填 `.pub`。預設私鑰必須位於 `~/.ssh`；自訂目錄要先列入 `.env` 的
`SSH_KEY_ALLOWED_DIRS`。

人工確認：

```bash
ssh -i ~/.ssh/dispatch_worker train@100.64.0.21
hostname
whoami
tmux -V
rsync --version
nvidia-smi          # 只有 GPU 機需要
exit
```

目前應用的 AsyncSSH 明確採 `known_hosts=None`（Tailscale 私網前提下的既有
取捨）；上面的人工 host-key 確認不代表應用會強制 known_hosts。改變這項政策
屬於 `INV-SSH-8` 架構決策。

### 8.4 先以 disabled 狀態登記

推薦在網頁「伺服器」分頁操作：

1. 新增伺服器，先設 `enabled=false`。
2. 按「測試 SSH」；它只會執行固定、唯讀的探測命令。
3. 建立新增請求，到「總覽」核准。
4. 系統會備份並原子寫入 `servers.yaml`、立即 hot reload，不需要重啟。
5. 測試無誤後，再建立更新請求將 `enabled=true`。

等價的 YAML 範例：

```yaml
servers:
  - name: worker-gpu-01
    host: 100.64.0.21
    user: train
    key: ~/.ssh/dispatch_worker
    port: 22
    gpu: true
    idle_gpu_util: 15
    idle_load: 2.0
    tags: [gpu, training]
    project_roots: [~/projects]
    dataset_roots: [~/datasets]
    enabled: false
```

如果不走上述網頁 approval 流程、而是由操作者直接編輯
`servers.yaml`，修改不會自動 hot reload；編輯後要重啟服務，或以已認證
請求呼叫 `POST /server-config/reload`。只有核准網頁建立／更新請求的路徑會在
落地後自動 reload。

`name` 只接受 `A-Za-z0-9_-`；不建議 root，預設也會拒絕。掃描 root 不可設成
`/`、`/etc`、`/var`、`/root`、`/usr`、`/opt`、`/tmp`，也不可直接設成
`~`／`/home`；使用 `~/projects`、`~/datasets` 這類窄路徑。

### 8.5 Canary 驗收

啟用後等待一個 monitor interval（預設 20 秒），確認總覽卡片在線，再建立
一個低風險 canary，例如 `hostname && id && pwd`。預設
`WEB_DIRECT_EXECUTE=true` 時，網頁派工會在同一個請求內建立並核准
approval record；若這次 onboarding 要求第二次明確人工點擊，先在 `.env`
設 `WEB_DIRECT_EXECUTE=false` 並重啟，再到核准面板按「核准」。實際派工會：

1. 用 SFTP 將原始 command bytes 寫入 `agent_jobs/{id}/cmd.sh`。
2. 建立固定 launcher `run.sh`，以 detached `tmux job_<id>` 執行。
3. 只以 `exit_code` 哨兵判定終態；SSH 暫時連不上是 unknown／跳過，不會誤判 failed。

「測試 SSH」只證明固定探測可用；canary 才會同時驗證 SFTP、tmux、home 權限與
哨兵 reconciliation。

### 8.6 未來真正導入 Node Agent 前必須先決定

若要實作 Node Agent，請另開架構 Goal 並明文核准：

- 拆分／修訂 `INV-SSH-1`，但 SSH backend 必須保留作相容與 rollback。
- 新增封閉的 `INV-NODE-*`：獨立可撤銷 node identity、outbound HTTPS polling、
  non-root roots、lease/ack、durable attempt、無 command interpolation、restart
  idempotency、heartbeat expiry=unknown、stop/result/log protocol。
- 先建立 immutable JobSpec／ExecutionPlan／RunAttempt 與 ExecutionBackend seam；
  不可在 agent 可能已 launch 時自動改走 SSH 重複執行。
- node enrollment、credential rotation/revocation、backend routing 必須走 approval。
- 先用 fake protocol 測試，再以至少兩台非 production 節點完成 ordinary-job
  canary 與 rollback drill；之後才考慮遷移 Codex Runner。

在這些決策與程式真正完成前，沒有可安全執行的 `install-node-agent.sh`。

## 9. 常見問題

| 現象 | 原因與處理 |
|---|---|
| 指令被拒絕「偵測到黑名單指令」 | 防呆黑名單刻意過殺——指令**文字上**含 rm -rf/dd/reboot 等就擋（連 `echo "rm -rf"` 也擋）。換寫法繞開字樣 |
| API 回 401 | OIDC 啟用時先按上方「登入」；session 過期/已撤銷就重登。Legacy 模式則檢查 `X-Auth-Token`，網頁重新輸入 token |
| OIDC 登入後又回到「尚未登入」 | 確認瀏覽器使用 HTTPS、IdP 登記的 redirect URI 與 `OIDC_REDIRECT_URI` 完全相同，且 reverse proxy 沒有改寫/丟掉 callback cookie |
| 任務一直 queued | 依序檢查：所有機器都忙/離線？`require_tag` 沒有機器符合？需要的資料集沒快取在任何機器（自動模式）？依賴的任務還沒完成？ |
| 任務 blocked | 它依賴的 sync/setup 任務失敗了。看那個任務的 log，修好後重跑 |
| 對話回「本地模型暫不可用」 | vLLM 沒起來或 `.env` 的 `VLLM_BASE_URL`/`VLLM_MODEL`/`VLLM_API_KEY` 不對。用 `{"cmd":"vllm"}` 打 `/agent/cmd` 看健康狀態 |
| vLLM 回 model not found | `.env` 的 `VLLM_MODEL` 要跟 vLLM 實際服務的名字一致（以文件建議的 8001 為例：`curl -H "Authorization: Bearer <key>" http://127.0.0.1:8001/v1/models`） |
| 收不到信 | SMTP 設定不完整會直接跳過（看 journalctl log）；只支援 STARTTLS，Gmail 用 587 埠 |
| 機器顯示離線但明明活著 | SSH 連不上（金鑰、防火牆、Tailscale 斷線）。手動 `ssh -i 金鑰 user@host` 排查 |
| `quickstart.sh` 說 port 已被占用 | 先跑 `./quickstart.sh status`；也檢查是否同時啟動了舊 `start.sh`、systemd 或手動 `python -m app.main`。腳本不會接管外部 PID |
| 找不到 Node Agent 安裝檔 | 目前尚未實作；請依 §8 使用 SSH worker。`app/agent_runtime.py` 不是遠端 Node Agent |

## 10. 安全注意事項

- 調度中心只綁私網（127.0.0.1 或 Tailscale IP），**永遠不要**設 0.0.0.0。
- `quickstart.sh` 自動建立的 open-development 設定只適合 loopback；對私網提供
  服務前必須先設定 `AUTH_TOKEN` 或完整 OIDC。正式常駐用 systemd，不要同時
  使用多個 process manager。
- 若 vLLM 只供同台 Server A 的調度中心使用，建議以
  `--host 127.0.0.1 --port 8001` 啟動（port 以你的 `VLLM_BASE_URL`
  為準）；若必須綁私網位址，要同時設定 vLLM API key 與防火牆。
- `.env` 含 token 與 SMTP 密碼，不要提交進任何版本控制、不要外傳。
- OIDC client secret 要用部署環境的 secret manager 注入。反向代理、
  load balancer、APM/WAF 不得記錄 callback query/redirect `Location`、request
  `Cookie`/`Authorization`/`X-Auth-Token`、response `Set-Cookie`，也不得記錄
  provider token-exchange request/response body。callback query 內有一次性
  authorization code/state；應用的 Uvicorn filter 只遮蔽 callback target，
  不能保護上游 header/egress trace。應用不儲存 provider access/refresh token。
- 稽核紀錄在 `audit.jsonl`（網頁事件流 = 它的尾段），出事時先看這裡。

**Node Agent 的三道煞車(Goal 3 C3)**:要真的讓任務跑在 agent 上,必須**三個
條件同時成立**,少一個都不會有任何任務流過去:

1. `.env` 設 `NODE_AGENT_V1_ENABLED=true`(協議與登錄可用)
2. `servers.yaml` 那台設 `execution_backend: node`(這台改走 agent 通道)
3. `.env` 設 `NODE_CANARY_REQUIRE_TAG=某個標籤`,**而且**你要把想跑的任務的
   `require_tag` 設成完全一樣的值

第 3 條是刻意的——**預設是空字串,代表沒有任何任務合格**。所以就算你不小心
開了前兩個,正式訓練也不會突然被丟到還沒驗證過的通道上。而且只有普通任務
(train/adhoc)能參加;Codex coding 任務、資料同步、環境設定任務永遠不合格。

**憑證換發**:agent 憑證外洩或要定期更換時,用「換發」而不是「撤銷再登錄」
——換發保留同一個 node 身分,正在跑的工作不會變成沒有主人;撤銷則會。兩者都
走核准流程,新憑證一樣只顯示一次。

**agent 怎麼裝**:`agent/dispatch-node-agent.service` 是 systemd 使用者單元,
以你自己的帳號執行(不是 root)、只主動對外連線、不在工作機上開任何連接埠。
安裝指令寫在那個檔案開頭的註解裡。
