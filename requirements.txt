fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
asyncssh>=2.14,<3.0
PyYAML>=6.0,<7.0

# 階段 5（選用）：LLM 自然語言排程／失敗診斷／信件摘要。app/llm.py 用
# try/except 包住這個 import——沒裝這個套件、或沒設定 ANTHROPIC_API_KEY，
# 整個系統照常運作（規則式後備／診斷不可用／信件不加摘要），見 README §7。
anthropic>=0.34,<1.0

# 階段 7：本地 vLLM Agent Layer（app/llm_local.py）用 httpx 呼叫
# OpenAI-compatible 的 /chat/completions，取代 openai SDK；同時也是 FastAPI
# TestClient（fastapi.testclient）本身的依賴，原本只在「開發/測試用」段，
# 階段 7 起是正式執行期依賴（VLLM_BASE_URL/VLLM_MODEL 沒設定時完全不會用到
# 網路功能，只是 import 本身要能成功）。
httpx>=0.27

# Goal 1 / Slice 7: production OIDC Authorization Code + PKCE provider,
# including JOSE/JWKS signature and standard ID-token claim validation.
Authlib>=1.7,<2.0

# 階段 9（選配）：MCP Bridge（app/mcp_bridge.py），只有要跑
# `python -m app.mcp_bridge`（ChatGPT custom connector 串接）時才需要；
# 調度中心本體（app/main.py）完全不 import 這個套件，沒裝也不影響
# 調度中心本身的任何功能（見 PLAN.md J 節、app/mcp_bridge.py 模組
# docstring）。
mcp>=1.28,<2.0

# 開發/測試用
pytest>=8.0
pytest-asyncio>=0.23
